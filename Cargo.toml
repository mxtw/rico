[package]
name = "rico"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version =  "4.5.32", features = ["derive"]}
nix = { version = "0.29.0", features = ["sched", "user", "fs", "mount"]}
